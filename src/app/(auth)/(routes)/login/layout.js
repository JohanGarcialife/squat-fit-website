// Componente de cliente: el título vive aquí.
export const metadata = {
  title: 'Iniciar sesión',
  description:
    'Entra en tu cuenta de Squad Fit para acceder a tu programa, tus cursos y tu recetario.',
};

export default function LoginLayout({ children }) {
  return children;
}
